apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: englishflow
  labels:
    app: api-gateway
spec:
  type: LoadBalancer # Change to ClusterIP if using Ingress
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: api-gateway
